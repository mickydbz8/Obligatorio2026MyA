@{
    ViewData["Title"] = "InspecciÃ³n RÃ¡pida de Campo (IA)";
}

<div class="container mt-4">
    <div class="text-center mb-5">
        <h2 class="text-info fw-bold"><i class="bi bi-robot"></i> Asistente de Campo con IA</h2>
        <p class="lead">SimulaciÃ³n de la interfaz Manos Libres y VisiÃ³n Computacional para uso con traje de apicultor.</p>
    </div>

    @if (TempData["MensajeVoz"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
            <i class="bi bi-mic-fill"></i> @TempData["MensajeVoz"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["MensajeCamara"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show shadow-sm" role="alert">
            <i class="bi bi-camera-fill"></i> @TempData["MensajeCamara"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row text-center mt-5">
        <div class="col-md-6 mb-4">
            <div class="card shadow border-0 rounded-4">
                <div class="card-body py-5">
                    <h1 class="display-1 text-primary mb-3"><i class="bi bi-mic"></i></h1>
                    <h4 class="card-title">Dictado de Voz</h4>
                    <p class="card-text text-muted mb-4">
                        Toca el botÃ³n para simular que MatÃ­as estÃ¡ dictando observaciones sobre una colmena usando el micrÃ³fono de su mÃ³vil.
                    </p>
                    <form asp-action="SimularVoz" method="post">
                        <button type="submit" class="btn btn-primary btn-lg rounded-pill px-5 shadow-sm">
                            <i class="bi bi-play-circle-fill"></i> Simular Dictado
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-6 mb-4">
            <div class="card shadow border-0 rounded-4">
                <div class="card-body py-5">
                    <h1 class="display-1 text-warning mb-3"><i class="bi bi-camera"></i></h1>
                    <h4 class="card-title">AnÃ¡lisis de Material</h4>
                    <p class="card-text text-muted mb-4">
                        Toca el botÃ³n para simular que la cÃ¡mara analiza el estado de un cajÃ³n y genera un diagnÃ³stico preventivo.
                    </p>
                    <form asp-action="SimularCamara" method="post">
                        <button type="submit" class="btn btn-warning btn-lg rounded-pill px-5 shadow-sm text-dark">
                            <i class="bi bi-camera-fill"></i> Simular AnÃ¡lisis
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

