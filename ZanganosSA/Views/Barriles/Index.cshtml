@model IEnumerable<ZanganosSA.Models.Barril>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoLote)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PesoNeto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaEnvasado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cosecha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoLote)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PesoNeto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaEnvasado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cosecha.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
